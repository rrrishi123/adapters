name: ci

# The same gate http-mcp has carried since July, now on every arm of the
# four-system (v0.0.2 audit: CI to all arms): build, vet, test, gofmt.
on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache: false # root is stdlib-only; only the nested webrtc module has a go.sum
      - run: go build ./...
      - run: ./build.sh # the shippable .bin/ layout, CI-verified
      - run: go vet ./...
      - run: go test ./...
      - run: go build ./... && go vet ./... && go test ./...
        working-directory: webrtc
      - run: gofmt -l . | tee /tmp/fmt && test ! -s /tmp/fmt
